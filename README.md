# dotfiles

Personal configuration files.

## Shell aliases

Git aliases are stored in `home/.zshrc`:

```sh
alias gs="git status"
alias ga="git add"
alias gc="git commit"
```

To use them manually:

```sh
source home/.zshrc
```
